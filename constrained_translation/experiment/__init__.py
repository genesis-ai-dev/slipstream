"""constrained_translation.experiment — Reproducible multilingual evaluation harness.

Submodules:
    manifest   — deterministic held-out manifest builder
    runner     — paired condition runner (semantic-only vs semantic+coverage)
    analysis   — metric analysis with bootstrap CIs and sign-flip p-values
    cli        — `experiment` subcommand CLI (prepare / run / analyze)
"""
